// Optional JavaScript file for interactivity
// Right now this just logs when the page loads

document.addEventListener("DOMContentLoaded", () => {
    console.log("Main page loaded successfully!");
});
